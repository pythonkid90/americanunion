To-do:
- Undo and redo buttons for AU website (ctrl-z and ctrl-y), probably on riight corner, do using backup system or using JS list
- Give previous hex already put in input when recoloring nation rather than just whatever was already in input
- Finish up all election scripts and get Python wrapper for rust API

